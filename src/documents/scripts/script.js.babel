'use strict'

let bb = {}
const $ = window.$

bb.getCheckedTags = () => {
  let checked = []
  $('#tags').find('input:checked').each(function () {
    checked.push($(this).attr('name'))
  })
  return checked
}
